import { type NextPage } from "next";

import { Layout } from "@/components/Layout";

const Home: NextPage = () => {
  return (
    <Layout pageTitle="Home">
      <>hello</>
    </Layout>
  );
};

export default Home;
